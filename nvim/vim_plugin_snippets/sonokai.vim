let g:sonokai_style = 'atlantis' " `'default'`, `'atlantis'`, `'andromeda'`, `'shusia'`, `'maia'`,`'espresso'`
let g:sonokai_better_performance = 1
let g:sonokai_disable_italic_comment = 1 "disable italic comment

colorscheme sonokai

hi! link @preproc Comment
